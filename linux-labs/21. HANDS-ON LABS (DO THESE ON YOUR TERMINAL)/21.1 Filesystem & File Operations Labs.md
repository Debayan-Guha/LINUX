# 21.1.1 Create and Navigate Directory Structure

## 21.1.1.1 Create directory tree `/tmp/devops-lab/project/` with subdirectories `src/`, `logs/`, `backup/`, `config/`

<img width="1366" height="768" alt="image" src="https://github.com/user-attachments/assets/8f09498f-0411-4471-8f99-e6c75f243d09" />

- `-p` : Creates parent folders if they do not exist.
- `/` : means in the root directory
- `~` : means in the home directory


## 21.1.1.2 Navigate into `project/src/` using absolute path
<img width="828" height="117" alt="image" src="https://github.com/user-attachments/assets/fb173882-f95c-4746-b85c-efce71cffb0e" />

- Starts with `/` : An absolute path must always start with a forward slash `(/)`, telling the system to look from the main system root directory regardless of where you are standing right now.

## 21.1.1.3 Navigate back to `project/` using relative path
<img width="828" height="117" alt="image" src="https://github.com/user-attachments/assets/42981927-2a09-47eb-85cc-f678f058a43b" />
<img width="831" height="125" alt="image" src="https://github.com/user-attachments/assets/40428044-62ee-446a-a757-4e6044d0705a" />

- `cd ..` : Moves you up one level into the immediate parent directory. (In this case, it takes you from `src/` back up to `project/`).
- `cd ../..` : Moves you up two levels into the grandparent directory. (From `src/`, this would skip `project/` and take you all the way back to `devops-lab/`).

## 21.1.1.4 Print current working directory at each step 
<img width="849" height="266" alt="image" src="https://github.com/user-attachments/assets/a41bef72-1df2-4ea2-8da8-6aee1470c8cf" />


## 21.1.1.5 Create empty files: `app.py`, `Dockerfile`, `nginx.conf` inside `config/`
<img width="1189" height="561" alt="image" src="https://github.com/user-attachments/assets/0ca608bf-5ff5-45f7-ac39-1ce9531ed121" />

## 21.1.1.6 Copy `nginx.conf` to `backup/nginx.conf.bak`
<img width="1182" height="646" alt="image" src="https://github.com/user-attachments/assets/ff831ee3-4ca7-4c5a-9b0f-5c08191d0a03" />

## 21.1.1.7 Move `app.py` to `src/app.py`
<img width="1186" height="683" alt="image" src="https://github.com/user-attachments/assets/8f8e2ebe-6b87-4373-ba13-f7d7113f8f7c" />

## 21.1.1.8 Create a symbolic link `config/nginx-link.conf` pointing to `config/nginx.conf`
<img width="1189" height="697" alt="image" src="https://github.com/user-attachments/assets/39bcce26-0306-4adb-8a8d-00d29a9bc28b" />

When multiple services or processes need to access the same file simultaneously, instead of duplicating the file everywhere, we create a single reference called a link. If we copy the file to multiple locations, each copy becomes an independent file. Editing one copy does NOT affect the others — they stay outdated because they are separate files, not connected. Links solve this problem because they do NOT duplicate the file's data. Instead, a link is just a pointer that tells the system, 'Go to the original file.' Since all links point to the same original file, any edit made to the original — OR through any link — changes the source file itself. Because all links reference this single source, the change is reflected simultaneously and instantly across all services accessing it.

***Note: If processes do NOT need to access the same file simultaneously, then a single file can be used directly without creating links — there is no risk of inconsistency since only one process uses it at a time.***

Why multiple processes can't always access the same file directly:

- If two processes write to the same file at the same time, one process may overwrite the other's data.
- If one process reads while another writes, the reader may get incomplete or inconsistent data.
- File locking mechanisms can help, but they slow down performance and are complex to manage.
- That's why we need either links or proper synchronization mechanisms to manage shared file access."



## 21.1.1.9 Delete the `logs/` directory (which is empty at this point)
<img width="685" height="570" alt="image" src="https://github.com/user-attachments/assets/abc74ab7-4491-4972-9cd6-14ddaeab7ae2" />


## 21.1.1.10 Verify the final directory structure using `tree` or `find`
<img width="685" height="271" alt="image" src="https://github.com/user-attachments/assets/e706b1dd-bcb4-4006-be77-fb58936e94db" />

- `find` : Real-time file search with filters (name, size, time, permissions) + actions (delete, execute, copy)


---


# 21.1.2 File Permissions Lab

## 21.1.2.1 Create a file `secret.sh` with content `#!/bin/bash\necho "Hello"`
<img width="749" height="652" alt="image" src="https://github.com/user-attachments/assets/b73c5904-4508-4450-a3bc-b9969bb8e315" />

## 21.1.2.2 Set permission so only owner can read, write, and execute (700)


**📊 Permission Values (The Numbers)**

| Symbol | Value | Meaning |
|--------|-------|---------|
| **r** | **4** | Read - View file contents |
| **w** | **2** | Write - Modify file contents |
| **x** | **1** | Execute - Run file as program |
| **-** | **0** | No permission |


**👥 The Three Permission Groups**

| Group | Who It Represents |
|-------|-------------------|
| **User (u)** | The file owner (creator) |
| **Group (g)** | Users in the file's group |
| **Others (o)** | Everyone else (not owner or group) |

<img width="749" height="161" alt="image" src="https://github.com/user-attachments/assets/dfde4b83-0a1b-429e-8a62-3566eaa54585" />

**Decoding `-rw-rw-r-- 1 debayan debayan 26 Jun 23 18:55 project/src/secret.sh`**

| # | Field | Value | Meaning |
|---|-------|-------|---------|
| 1 | File Type | `-` | Regular file (not a directory) |
| 2 | User Permissions | `rw-` | Owner can **read** and **write**, not execute |
| 3 | Group Permissions | `rw-` | Group members can **read** and **write**, not execute |
| 4 | Others Permissions | `r--` | Others can **read only** |
| 5 | Hard Links | `1` | Only one link to this file|
| 6 | Owner | `debayan` | User who owns the file |
| 7 | Group | `debayan` | Group that has group permissions |
| 8 | Size | `26` | File size in bytes |
| 9 | Date | `Jun 23` | Last modified date |
| 10 | Time | `18:55` | Last modified time (6:55 PM) |
| 11 | Filename | `secret.sh` | Name of the file |


## 21.1.2.3 Create another file `team.sh` with same content
<img width="694" height="646" alt="image" src="https://github.com/user-attachments/assets/21ed7611-f8a4-4c16-977d-3784ed22d9c6" />

## 21.1.2.4 Change group ownership to a group that exists on your system
<img width="924" height="200" alt="image" src="https://github.com/user-attachments/assets/7aae4427-9984-4749-9dcd-cceaa01b8635" />

## 21.1.2.5 Set permission so group can read and execute (750)
<img width="924" height="200" alt="image" src="https://github.com/user-attachments/assets/ccdb74c1-bfd6-4d61-adf5-e3111d723519" />

## 21.1.2.6 Create a directory shared/ with sticky bit (1777)
<img width="1366" height="768" alt="image" src="https://github.com/user-attachments/assets/d5c91017-7766-4e8a-82e2-1703da576b3f" />

**What is sticky bit**

In a sticky bit `(t)` directory, only the file owner, directory owner, or root can delete or rename files — even if others have write access.


## 21.1.2.7 Create a file suid-binary and set SUID bit (4755)
<img width="788" height="477" alt="image" src="https://github.com/user-attachments/assets/7b7dd3f8-8fe0-4cfa-8c6b-d82f32ed0dcf" />

**What is SUID**

When a file with SUID `(s)` is executed, it runs with the permissions of the file OWNER, not the user who runs it.



## 21.1.2.8 Verify all permissions using `ls -l`
<img width="732" height="182" alt="image" src="https://github.com/user-attachments/assets/75095666-77c4-4030-9775-d0f0f9212111" />

## 21.1.2.9 Try to execute secret.sh as a different user (observe permission denied)
<img width="724" height="115" alt="image" src="https://github.com/user-attachments/assets/56d85237-0194-4af6-95f4-770338360ab5" />

- `sudo -u nobody` : Tells Linux to run the upcoming command as the nobody user account instead of your debayan account.
- `nobody` : A standard, heavily restricted system user that exists on all Ubuntu machines specifically for testing permissions.


## 21.1.2.10 Remove all created files and directories
<img width="709" height="148" alt="image" src="https://github.com/user-attachments/assets/41afc3da-9d88-4078-a868-75b12e44e3e2" />

















